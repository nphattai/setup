# Secrets — NEVER commit real env values
.env
.env.*
!*.env.example
*.local
.dotenvx*
# Node / build
node_modules/
dist/
.DS_Store
# Worktrees live outside the repo (../wt) but ignore if symlinked
wt/

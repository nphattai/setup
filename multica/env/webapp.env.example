# Webapp (NON-PROD/dev). Copy to your injector's source; fill dev values. NEVER commit real secrets.
# API codegen targets (BE running locally)
USER_API_URL=http://localhost:3333
ADMIN_API_URL=http://localhost:3333
# FE → BE base URLs
NEXT_PUBLIC_API_URL=http://localhost:3333
# Auth / 3rd-party (dev/sandbox keys only)
NEXT_PUBLIC_AUTH_DOMAIN=
# Mobile (Expo) — dev
EXPO_PUBLIC_API_URL=http://localhost:3333
